// Intentionally empty. ZoneSequencer's methods are inline stubs in the header — the unit
// suite never calls zone_sequencer (only seqPublish does, and it is not exercised
// headlessly). logos_sdk.cpp #includes this file, so it must exist.
